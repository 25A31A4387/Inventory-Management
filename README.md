# Inventory Management System in C

A simple inventory management system for a small shop using C language.

## Features
- Add, view, update, and delete products
- Uses file handling for data storage

## How to Compile & Run
```bash
gcc inventory.c -o inventory
./inventory
